# Version JS pack — Node 18

These `.js` files encode **only** Customer Version 18 syntax (ES2022 / Node.js 18).

| File | Role |
|---|---|
| `features.js` | CJS helpers for Mocha / nyc / stryker / lizard |
| `featuresBrowser.js` | ESM helpers bundled by Vite |
| `runtime.js` | Version metadata + Node engine guard |
